import 'package:flutter/material.dart';
class Somma extends StatefulWidget {
  @override
  _SommaState createState() => _SommaState();
}

class _SommaState extends State<Somma> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('somma'),),
    );
  }
}